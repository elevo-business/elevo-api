{
  "name": "elevo-api-proxy",
  "version": "1.0.0",
  "description": "ELEVO API Proxy — Pipedrive CRM Integration",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
